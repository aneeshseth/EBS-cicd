name: deploy api
on:
  push:
    branches:
      - main
      
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Create zip
        run: "cd server && npm install && npm run build && zip -r deploy.zip *"

      - name: Deploy to EB
        uses: einaregilsson/beanstalk-deploy@v20
        with:
          aws_access_key: AKIA35VTPTMRG2Z7AZHR
          aws_secret_key: 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
          application_name: aws-beanstalk-env
          environment_name: aws-beanstalk
          version_label: ${{ github.sha }}
          region: us-west-1
          deployment_package: ./server/deploy.zip



 